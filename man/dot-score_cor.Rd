% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_engines.R
\name{.score_cor}
\alias{.score_cor}
\title{Per-pair matching engines: \code{cor} / \code{fft} / \code{dtw}}
\usage{
.score_cor(mat_soundscape, mat_template)
}
\arguments{
\item{mat_soundscape}{frames x bins.}

\item{mat_template}{frames x bins.}
}
\value{
numeric Pearson score per complete window.
}
\description{
The three coexisting scoring methods, each a \strong{pure compute on
supplied spectrogram matrices} (frames x frequency-bins) returning one score
per complete sliding window (the dispatcher \code{\link[=run_matching_i]{run_matching_i()}} pads/aligns and
wraps them into the frozen contract). Decisions: \verb{plans/plan-logs/plan-log-2026-06-07-001.md}
(Stage 4 gate). Read-only references: \code{R/original/run_matching_i.R} (\code{cor},
\code{dtw}) and \code{R/original/alt_run_matching/raw_fft_matching.R} (\code{fft}).
\itemize{
\item \code{.score_cor} — Pearson sliding-window (reference). Cross term via a BLAS
matrix loop over template frames; mean/variance via cumulative sums (C1.3).
\item \code{.score_fft} — \strong{same Pearson model}, the cross term via FFT (Lewis 1995
mean-centred NCC); the fast backend for \code{cor}, reproduces it within
tolerance (RMF-01..05).
\item \code{.score_dtw} — Dynamic Time Warping (\code{dtwclust::dtw_basic}), with an
absolute, warp-normalized score (RMD-02, SR-02 modo B: \code{symmetric2}).
\code{cor} engine — Pearson per window (reference). RMC + C1.3.
}
}
