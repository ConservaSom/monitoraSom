% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_peaks.R
\name{filter_detections}
\alias{filter_detections}
\title{Apply per-template detection thresholds in batch (AFL-16)}
\usage{
filter_detections(
  detec,
  thresholds,
  key = "template_name",
  scope = c("grid", "pair")
)
}
\arguments{
\item{detec}{a detections table (standard schema; coerced).}

\item{thresholds}{a tidy data.frame, one row per template: a \code{key} column plus
any of \code{min_score} / \code{min_quant} / \code{top_n} (an \code{NA} cell skips that filter for
that template).}

\item{key}{the column joining \code{thresholds} to \code{detec}. Default \code{"template_name"}.}

\item{scope}{\code{"grid"} (default; the quantile / \code{top_n} are computed within each
template's pooled detections) or \code{"pair"} (per-pair \code{peak_quant}). Forwarded
to the internal \code{filter_detections_i()}.}
}
\value{
a tibble of filtered detections (standard schema), row-bound across the
listed templates.
}
\description{
Closes the diagnostics -> filter loop: applies a tidy table of
per-template thresholds to the \strong{raw} detections in one pass, reusing
the internal \code{filter_detections_i()} per template. Thresholds typically come from
\code{\link[=diagnostic_validations]{diagnostic_validations()}} \code{score_cut} (supplied as \code{min_score}), but any
combination of \code{min_score} / \code{min_quant} / \code{top_n} columns is honoured. Only
templates listed in \code{thresholds} appear in the result.
}
\seealso{
\code{filter_detections_i()} (internal), \code{\link[=diagnostic_validations]{diagnostic_validations()}}.
}
