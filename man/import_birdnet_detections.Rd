% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_birdnet_table.R
\name{import_birdnet_detections}
\alias{import_birdnet_detections}
\title{Import BirdNET-Analyzer detection tables as monitoraSom detections}
\usage{
import_birdnet_detections(
  path,
  signals_db,
  min_conf = 0,
  format = NULL,
  origin = "birdnet"
)
}
\arguments{
\item{path}{Path to one BirdNET result file (\code{table} or \code{csv}).}

\item{signals_db}{Path to the \code{signals.duckdb} database (created if missing).}

\item{min_conf}{Numeric threshold on \code{Confidence}; rows with
\code{Confidence < min_conf} are dropped. Default \code{0} keeps every row
(lossless; BirdNET's own 0.1 export filter is a user export-time
decision).}

\item{format}{\code{"table"}, \code{"csv"}, or \code{NULL} (default) to auto-detect by
extension and header sniff.}

\item{origin}{Origin stamp for the \code{origin} column (default \code{"birdnet"}).}
}
\value{
The imported signals frame, invisibly.
}
\description{
Reads one BirdNET-Analyzer result file (\code{table} or \code{csv}),
converts it to the standard \code{signals} frame with \code{signal_class = "detection"}, and idempotently writes it into a \code{signals.duckdb} database
(re-importing updates the same rows). One row per BirdNET detection, in
the standard 44-column signals format. Returns the frame invisibly.
}
\details{
BirdNET-Analyzer's default result type is \code{table}: a tab-separated,
Raven-style selection table with the detection box (\verb{Begin Time (s)} /
\verb{End Time (s)}, \verb{Low Freq (Hz)} / \verb{High Freq (Hz)}), the species
(\verb{Common Name} / \verb{Species Code}) and a model \code{Confidence} score. The \code{csv}
result type is lossy (no frequency band, no channel) and is also
supported. The importer parses the file \strong{directly} (it does not reuse
\code{\link[=read_raven_selection]{read_raven_selection()}}); the channel vocabulary is the only part shared
with the Raven importer.

\strong{Semantic mapping (fixed):} BirdNET's \code{Confidence} column is a numeric
\strong{model score} (0–1), not a label category. It maps \strong{directly} to
\code{det_peak_score} (numeric). It does \strong{NOT} map to \code{roi_label_confidence}
(character category like \code{"certain"} in ROI data). The English
\verb{Common Name} is stored in the detection's free-text \code{roi_comment}; the
stable \verb{Species Code} (eBird code) becomes \code{roi_label},
and \code{det_template_name}.

A multiclass model such as BirdNET works like several single-class
models running side by side. From this import on, monitoraSom treats
every species (class) as its own "template", which is what the
validation and diagnostics steps expect; see \code{\link[=diagnostic_validations]{diagnostic_validations()}}
for the per-class reading. Diagnosing the model as a whole (all classes
together) is not available yet.

Detection rows are \code{signal_class = "detection"} and are \strong{never}
eligible as validation ground truth, so BirdNET rows validate only as
TP/FP candidates.

Importing the same file twice is safe: \code{signal_id} is a deterministic
sha256 over (normalized path, start, end, species code), so a re-import
updates the same rows instead of duplicating them.
}
\examples{
\donttest{
# Off-flow utility: import a synthetic BirdNET table into a temporary
# database.
# Load the package
library(monitoraSom)
f <- tempfile(fileext = ".BirdNET.results.table.txt")
hdr <- paste(c("Selection", "View", "Channel", "Begin Time (s)",
              "End Time (s)", "Low Freq (Hz)", "High Freq (Hz)",
              "Common Name", "Species Code", "Confidence"),
            collapse = "\t")
writeLines(c(hdr,
  "1\tSpectrogram 1\t1\t0.5\t1.5\t2000\t6500\tMyiothlypis flaveola\tmyifla\t0.87"
), f)
db <- tempfile(fileext = ".duckdb")
sig <- import_birdnet_detections(f, db)
sig[, c("roi_label", "roi_comment", "det_peak_score")]
}
}
\seealso{
\code{\link[=read_raven_selection]{read_raven_selection()}}, \code{\link[=validate_by_overlap]{validate_by_overlap()}}
}
