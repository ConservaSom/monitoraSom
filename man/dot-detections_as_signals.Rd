% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_signals.R
\name{.detections_as_signals}
\alias{.detections_as_signals}
\title{Legacy \code{detections} frame (27 cols) -> signals ("detection" class).
\code{signal_id == detection_id} (frozen tuple reused), so validation-app joins
and goldens survive untouched. An existing non-NA \code{detection_id} is
authoritative (kept unchanged — e.g. CRAN-98 legacy-CSV ids synthesized with
the template_file fallback); the tuple is recomputed only when NA.}
\usage{
.detections_as_signals(df)
}
\arguments{
\item{df}{Legacy detections frame (27-column standard detections schema).}
}
\description{
Legacy \code{detections} frame (27 cols) -> signals ("detection" class).
\code{signal_id == detection_id} (frozen tuple reused), so validation-app joins
and goldens survive untouched. An existing non-NA \code{detection_id} is
authoritative (kept unchanged — e.g. CRAN-98 legacy-CSV ids synthesized with
the template_file fallback); the tuple is recomputed only when NA.
}
