% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_annotations.R
\name{read_raven_selection}
\alias{read_raven_selection}
\title{Read a Raven selection table into ROI data}
\usage{
read_raven_selection(
  path,
  soundscape_path = NULL,
  label_col = NULL,
  roi_source = "import_raven"
)
}
\arguments{
\item{path}{Path to a Raven selection table (a tab-separated \code{.txt} file
with a header).}

\item{soundscape_path}{Optional recording path stamped on every row when the
table has no \verb{Begin Path}/\verb{Begin File} columns. Default \code{NULL} leaves those
fields \code{NA}.}

\item{label_col}{Optional name of the annotation column. Default \code{NULL}
auto-detects among \code{Annotation}/\code{Species} (case-insensitive); set it to
force a specific column (see the example below).}

\item{roi_source}{Origin stamp written to \code{roi_source} (default
\code{"import_raven"}; carried through unchanged in the \code{origin} column).}
}
\value{
ROI data in the standard format: \code{roi_start},
\code{roi_end}, \code{roi_min_freq}/\code{roi_max_freq} in kHz, \code{roi_label}, \code{roi_channel},
\code{roi_source}, \code{soundscape_file}, \code{soundscape_path}.
}
\description{
Reads a Raven selection table (tab-separated, with a header) and
returns it in the monitoraSom standard ROI format. Frequencies are converted from
Hz to kHz and every row is stamped as a manual annotation.
}
\details{
Raven exports often carry two rows per selection (a Waveform view and a
Spectrogram view); the frequency band lives on the spectrogram row, so when a
\code{View} column is present only the spectrogram rows are kept. \verb{Begin Time (s)}
and \verb{End Time (s)} are required; \verb{Low Freq (Hz)}/\verb{High Freq (Hz)} and
\code{Channel} are used when present and become \code{NA} otherwise. The source
recording is taken from the table's own \verb{Begin File}/\verb{Begin Path} columns
when they exist, else from \code{soundscape_path}.
}
\examples{
# Off-flow utility: round-trip ROIs out to Raven format and back in tempdir.
\donttest{
  # Load the package
  library(monitoraSom)
rois <- data.frame(
  roi_start = 1, roi_end = 2, roi_min_freq = 2, roi_max_freq = 6,
  roi_label = "sp1", soundscape_file = "rec.wav")
f <- file.path(tempdir(), "sel.txt")
write_raven_selection(rois, f)

back <- read_raven_selection(f)
back[, c("roi_start", "roi_end", "roi_min_freq", "roi_label")]

# Force a custom annotation column with label_col (no auto-detection):
tbl <- data.frame(
  "Begin Time (s)" = 1, "End Time (s)" = 2,
  "Low Freq (Hz)" = 2000, "High Freq (Hz)" = 6000,
  "Field Note" = "sp1", check.names = FALSE)
f2 <- file.path(tempdir(), "sel_custom.txt")
utils::write.table(tbl, f2, sep = "\t", quote = FALSE, row.names = FALSE)
back2 <- read_raven_selection(f2, label_col = "Field Note")
back2[, c("roi_start", "roi_end", "roi_label")]
}
}
\seealso{
\code{\link[=write_raven_selection]{write_raven_selection()}}, \code{\link[=read_audacity_labels]{read_audacity_labels()}},
\code{\link[=validate_by_overlap]{validate_by_overlap()}}
}
