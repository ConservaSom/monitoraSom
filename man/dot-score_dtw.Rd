% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_engines.R
\name{.score_dtw}
\alias{.score_dtw}
\title{\code{dtw} engine — Dynamic Time Warping (RMD-01/02/03).}
\usage{
.score_dtw(mat_soundscape, mat_template, dtw_slack = 0.2)
}
\arguments{
\item{mat_soundscape}{frames x bins.}

\item{mat_template}{frames x bins.}

\item{dtw_slack}{warping-window slack as a fraction of the template length
(RMD-03, exposed tuning knob; default \code{0.2} = the original constant).}
}
\value{
numeric comparable score per complete window.
}
\description{
Per window, a \code{dtwclust::dtw_basic} L1 distance between the
soundscape window and the template (both multivariate series, frames x bins).
Requires BOTH \code{dtw} and \code{dtwclust} (RMD-01). The raw distance is normalized to
an \strong{absolute, cross-soundscape- and cross-template-comparable} score
(RMD-02; SR-02 resolved in mode B, 2026-08-31): \code{dtw::symmetric2} with
\code{normalize = TRUE} (the standard path-length normalization, \code{dist / (N + M)}),
then divided by \code{bins} — the mean per-cell L1 cost along the ACTUAL
alignment — mapped through \code{1 / (1 + cost)} to a bounded \verb{(0, 1]} scale.
Because the denominator follows the real warp path (not a fixed template
area), the warp-length bias of the former \code{symmetric1} form is gone: a
no-warp alignment scores exactly as before, and heavier warping no longer
inflates the score. Scores are NOT comparable against \code{"cor"}/\code{"fft"}
(Pearson \verb{-1..1}) — thresholds are engine-specific (and per template).
}
