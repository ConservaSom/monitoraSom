% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_spectro.R
\name{extract_spectro}
\alias{extract_spectro}
\title{Compute spectrogram data from a Wave object}
\usage{
extract_spectro(
  rec,
  flim = NULL,
  tlim = NULL,
  ovlp = 50,
  wl = 1024,
  norm = FALSE,
  pitch_shift = 1,
  channel = c("left", "right", "mix"),
  use_fftw = FALSE,
  ...
)
}
\arguments{
\item{rec}{An object of class \code{"Wave"} (tuneR). Must be PCM-encoded; float
WAVs are rejected (see Details).}

\item{flim}{A numeric vector of length 2 giving the minimum and maximum
frequency limits to compute, in kHz. Must satisfy \code{flim[1] < flim[2]}.}

\item{tlim}{A numeric vector of length 2 giving the minimum and maximum
time limits to compute, in seconds. Must satisfy \code{tlim[1] < tlim[2]}.}

\item{ovlp}{A numeric value specifying the percentage overlap of windows
(0-100).}

\item{wl}{An integer FFT window length. Coerced to integer; a warning is
issued when it is not a power of 2 (FFTW is fastest with powers of 2).}

\item{norm}{A logical; if \code{TRUE}, amplitude is normalised by \code{seewave} so
the loudest bin is 0 dB and the per-bit-depth scaling is skipped.}

\item{pitch_shift}{A numeric display pitch factor; one of -8, -6, -4, -2, 1.
The size of the number (\code{abs()}) sets how much the time and frequency
axes are stretched. By convention, a negative sign also flips the
playback direction.}

\item{channel}{One of \code{"left"}, \code{"right"}, \code{"mix"}. Selects the channel of a
stereo recording (\code{"mix"} averages both). Ignored for mono input, except
\code{"right"} on mono input which emits a warning.}

\item{use_fftw}{A logical; if \code{TRUE}, use the \code{fftw} library (via \code{seewave})
for the FFT. Requires the \code{fftw} package. Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{seewave::spectro()}.}
}
\value{
An object of class \code{"SpectroResult"}: a list with \code{power_db}
(numeric matrix, freq x time, in dB, NOT clamped to any range),
\code{time_vec} (seconds), \code{freq_vec} (kHz) and \code{sample_rate} (Hz, after the
pitch-shift rescaling).
}
\description{
The computation half of \code{\link[=fast_spectro]{fast_spectro()}}: it runs
\code{seewave::spectro()} on a \code{Wave} recording and converts amplitude to
decibels, returning the raw spectrogram data \emph{without} drawing anything.
Pass the result to \code{\link[=plot_spectro]{plot_spectro()}} to render it. Splitting compute from
render lets you cache the \code{SpectroResult} (e.g. with \code{saveRDS()}) and
redraw it cheaply under different display settings.
}
\details{
Use this half directly only when you want the numeric spectrogram (a dB
matrix) or want to render the same data several ways without recomputing the
FFT; for a one-shot plot, \code{\link[=fast_spectro]{fast_spectro()}} is simpler. Restrict \code{flim}/\code{tlim}
to compute only the band and time window you care about, since it is
faster than cropping afterwards. Things to know: the input must be integer-PCM; float-encoded
(non-PCM) WAVs are rejected with an error, because \code{seewave}'s per-bit-depth
scaling assumes integer PCM and a float input would silently produce an
all-black spectrogram. \code{wl} is coerced to integer and warns when it is not a
power of 2 (FFT is fastest with powers of 2). The returned \code{power_db}
matrix keeps the full measured range; the display range is applied
later, in \code{\link[=plot_spectro]{plot_spectro()}}.
}
\section{Pipeline context}{

A utility used at multiple steps of the workflow, including the
segmentation and validation apps and \code{\link[=plot_scores]{plot_scores()}}. Reads a \code{Wave}
recording (from \code{tuneR::readWave()}). Produces a \code{SpectroResult} for
\code{\link[=plot_spectro]{plot_spectro()}}.
}

\examples{
# Off-flow utility: compute (but do not draw) the spectrogram of a recording.
\dontrun{
# Load the package
library(monitoraSom)
# (The package ships no field recordings; synthesize one to draw.)
wav <- file.path(tempdir(), "rec_01.wav")
rec <- tuneR::normalize(tuneR::sine(4000, duration = 3 * 16000,
                                    samp.rate = 16000), unit = "16")
tuneR::writeWave(rec, wav)
rec <- tuneR::readWave(wav)

sp <- extract_spectro(rec, flim = c(0, 10), ovlp = 50, wl = 1024)
dim(sp$power_db)   # frequency bins x time frames, in dB

# Restrict the computed band/window to speed up and focus:
extract_spectro(rec, flim = c(2, 8), tlim = c(0, 2))
}
}
\seealso{
\code{\link[=plot_spectro]{plot_spectro()}}, \code{\link[=fast_spectro]{fast_spectro()}}
}
