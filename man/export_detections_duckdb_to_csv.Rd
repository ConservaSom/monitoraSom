% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detections_duckdb.R
\name{export_detections_duckdb_to_csv}
\alias{export_detections_duckdb_to_csv}
\title{Export a DuckDB detections table to CSV (legacy back-compat converter)}
\usage{
export_detections_duckdb_to_csv(
  duckdb_path,
  csv_path,
  overwrite = FALSE,
  legacy = TRUE
)
}
\arguments{
\item{duckdb_path}{source detections \code{.duckdb}.}

\item{csv_path}{destination CSV.}

\item{overwrite}{overwrite an existing CSV (default \code{FALSE}).}

\item{legacy}{emit the original 21-column layout (default \code{TRUE}).}
}
\value{
invisibly, the exported data.frame.
}
\description{
Store-level utility for the detections store, exported for
advanced use (not part of the everyday pipeline). The sanctioned way to
get a CSV out of the detections store (the
refactored pipeline no longer writes CSV directly — Stage 4 gate). By default
it emits the \strong{legacy 21-column layout} the original \code{run_matching} wrote
(dropping \code{detection_id}/\code{template_id}/\code{score_method}), so existing
downstream code keeps working; set \code{legacy = FALSE} for the full standard
schema. Mirrors \code{\link[=export_templates_duckdb_to_csv]{export_templates_duckdb_to_csv()}}.

\strong{Data-loss boundary (G5 in plan-log-2026-06-22-001):} When \code{legacy = TRUE}
(the default), the three durable-key columns \code{detection_id}, \code{template_id},
and \code{score_method} are dropped. Consumers of this CSV \strong{cannot} reconstruct
the durable detection identity (idempotent upsert key) or join back to the
template database. Use \code{legacy = FALSE} when full provenance is needed.
}
\keyword{internal}
