% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_annotations.R
\name{read_audacity_labels}
\alias{read_audacity_labels}
\title{Read an Audacity label track into ROI data}
\usage{
read_audacity_labels(
  path,
  soundscape_path = NULL,
  roi_source = "import_audacity"
)
}
\arguments{
\item{path}{Path to an Audacity label track (tab-separated, no header).}

\item{soundscape_path}{Optional recording path stamped on every row. Default
\code{NULL} leaves the \verb{soundscape_*} fields \code{NA}.}

\item{roi_source}{Origin stamp written to \code{roi_source} (default
\code{"import_audacity"}; carried through unchanged in the \code{origin} column).}
}
\value{
ROI data in the standard format; frequencies in kHz.
}
\description{
Reads an Audacity label track (tab-separated, no header) and
returns it in the monitoraSom standard ROI format, converting any frequency
bounds
from Hz to kHz and stamping each row as a manual annotation.
}
\details{
A standard Audacity label is \verb{start<TAB>end<TAB>label}. Frequency-bearing
labels add a continuation line \verb{\\<TAB>lowHz<TAB>highHz} immediately after the
label (Audacity's extended format); those Hz bounds map to \code{roi_min_freq}/
\code{roi_max_freq} (kHz). Labels without a continuation get \code{NA} frequencies.
Malformed lines (fewer than three fields, or a stray continuation) are
skipped, and an empty or all-blank file yields empty ROI data rather than
an error.
}
\examples{
# Off-flow utility: round-trip ROIs out to Audacity format and back in tempdir.
\donttest{
rois <- data.frame(
  roi_start = 1, roi_end = 2, roi_min_freq = 2, roi_max_freq = 6,
  roi_label = "sp1", soundscape_file = "rec.wav")
f <- file.path(tempdir(), "labels.txt")
write_audacity_labels(rois, f)

back <- read_audacity_labels(f)
back[, c("roi_start", "roi_end", "roi_min_freq", "roi_label")]
}
}
\seealso{
\code{\link[=write_audacity_labels]{write_audacity_labels()}}, \code{\link[=read_raven_selection]{read_raven_selection()}},
\code{\link[=validate_by_overlap]{validate_by_overlap()}}
}
