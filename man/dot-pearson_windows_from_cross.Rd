% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_helpers.R
\name{.pearson_windows_from_cross}
\alias{.pearson_windows_from_cross}
\title{Mean-centred normalized cross-correlation (Pearson) from the cross term}
\usage{
.pearson_windows_from_cross(cross, mat_soundscape, mat_template)
}
\arguments{
\item{cross}{numeric per-window cross term (length n_complete).}

\item{mat_soundscape}{frames x bins.}

\item{mat_template}{frames x bins.}
}
\value{
numeric Pearson score per window; \code{NA} where a window has zero
variance (filled with the sentinel by \code{\link[=.pad_score_to_length]{.pad_score_to_length()}}).
}
\description{
Shared combine for \verb{_cor} and \verb{_fft}: given the per-window cross
term \verb{cross[k] = sum_\{f,b\} template[f,b] * soundscape[k+f-1, b]}, returns the
Pearson coefficient of each window's flattened patch against the flattened
template — identical to \code{cor(c(window), c(template))}. The local window mean
and sum-of-squares come from cumulative sums (O(n)), so only the \code{cross} term
differs between the BLAS (\verb{_cor}) and FFT (\verb{_fft}, Lewis 1995) backends.
}
