% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_example_data.R
\name{is_example_data_cached}
\alias{is_example_data_cached}
\title{Is the downloaded example data already cached?}
\usage{
is_example_data_cached()
}
\value{
A logical scalar: \code{TRUE} if every expected file of the example
project is present in the cache, \code{FALSE} otherwise.
}
\description{
Reports whether the example project (the
\code{basileuterus-culicivorus} walkthrough corpus: 14 WAVs, \code{rois.duckdb}, the
project marker and the walkthrough in three formats, kept outside the
package to stay within CRAN size limits) has already been downloaded to
the local cache by \code{\link[=fetch_example_data]{fetch_example_data()}}. It is a pure,
side-effect-free check: it touches no network and creates nothing.
}
\details{
Use it to guard optional example/vignette code so it runs only when the
data is present; this pattern keeps the package's checks network-free:
\code{if (is_example_data_cached()) { ... }}. The example lives under
\code{\link[tools:R_user_dir]{tools::R_user_dir()}}\verb{("monitoraSom", "cache")}, in the
\code{basileuterus-culicivorus} subdirectory that \code{\link[=fetch_example_data]{fetch_example_data()}}
returns.
}
\examples{
# Non-destructive check; FALSE on a machine that has never fetched the data.
library(monitoraSom)
is_example_data_cached()
}
\seealso{
\code{\link[=fetch_example_data]{fetch_example_data()}} to populate the cache.
}
