% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching.R
\name{estimate_matching}
\alias{estimate_matching}
\title{Estimate the size of a matching run before launching it}
\usage{
estimate_matching(df_grid, quiet = FALSE)
}
\arguments{
\item{df_grid}{A \code{\link[=fetch_match_grid]{fetch_match_grid()}} result.}

\item{quiet}{Logical. When \code{TRUE}, return the figures without printing them.
Default \code{FALSE}.}
}
\value{
Invisibly, a one-row \code{data.frame} with \code{n_pairs}, \code{n_soundscapes},
\code{n_templates} and \code{total_scan_seconds} (\code{NA} when \code{soundscape_duration} is
absent from the grid).
}
\description{
Reports how big the next \code{\link[=run_matching]{run_matching()}} call will be: the number
of pairs, soundscapes and templates, and, when \code{soundscape_duration} is on
the grid, the total soundscape audio to scan. Purely informational:
\code{\link[=run_matching]{run_matching()}} prints this automatically and proceeds with no confirmation
gate, so call it directly only when you want a standalone dry-run estimate.
}
\details{
Use it to sanity-check a grid before committing to a long run: a
surprising pair count usually means the grid crossed more templates or
soundscapes than intended. It reads only the grid, never the audio, so it is
instant.
}
\section{Pipeline context}{

Companion to step 7. Reads the search grid from \code{\link[=fetch_match_grid]{fetch_match_grid()}}
(step 6). Produces run-size figures only (no pipeline artifact).
}

\examples{
\dontrun{
# Dry-run size estimate for a grid, before scoring it.
data(df_grid)
estimate_matching(df_grid)
}
}
\seealso{
\code{\link[=fetch_match_grid]{fetch_match_grid()}}, \code{\link[=run_matching]{run_matching()}}, \code{\link[=summarise_matching]{summarise_matching()}}.
}
