% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_helpers.R
\name{.spec_params_from_row}
\alias{.spec_params_from_row}
\title{seewave spec_params for one grid row (RMC-12: \code{norm = FALSE} — F3, 2026-09-10:
the pre-1.2.0 front-end kept \code{norm = TRUE} as an uninspected dev artifact,
making scores relative to each recording's own maximum. \code{norm = FALSE} keeps
absolute amplitude, so scores compare across recordings.)}
\usage{
.spec_params_from_row(df_grid_i, side = c("soundscape", "template"))
}
\arguments{
\item{df_grid_i}{one row of the match grid.}

\item{side}{\code{"soundscape"} (default) reads \code{soundscape_channel}
from the row; \code{"template"} reads \code{template_channel}.}
}
\value{
list passed to \code{seewave::spectro} (no \code{tlim}).
}
\description{
seewave spec_params for one grid row (RMC-12: \code{norm = FALSE} — F3, 2026-09-10:
the pre-1.2.0 front-end kept \code{norm = TRUE} as an uninspected dev artifact,
making scores relative to each recording's own maximum. \code{norm = FALSE} keeps
absolute amplitude, so scores compare across recordings.)
}
