% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_annotations.R
\name{write_raven_selection}
\alias{write_raven_selection}
\title{Write ROI data as a Raven selection table}
\usage{
write_raven_selection(rois, path)
}
\arguments{
\item{rois}{A data.frame of ROIs. Required columns: \code{roi_start}, \code{roi_end}
(seconds) and \code{roi_label}; optional \code{roi_min_freq}/\code{roi_max_freq} (kHz,
written as Hz) add the frequency band, \code{roi_channel} maps to the Raven
\code{Channel} column (\code{"right"} -> 2, else 1), and \code{soundscape_file} is written
to \verb{Begin File} so the round trip restores provenance (DEC-18). Any
\code{\link[=fetch_rois]{fetch_rois()}} output qualifies.}

\item{path}{Output file path for the Raven selection table.}
}
\value{
\code{path}, invisibly (the function is called for its file side effect).
}
\description{
Exports a monitoraSom ROI table to a Raven selection
table, so ROIs can be opened and edited in Raven. Frequencies are converted
from kHz back to Hz. The inverse of \code{\link[=read_raven_selection]{read_raven_selection()}}.
}
\details{
Rows are written with \code{View = "Spectrogram 1"} and a \code{Channel} column mapped
from \code{roi_channel} (\code{"right"} -> 2, everything else -> 1). The output is
tab-separated with a header, the format \code{\link[=read_raven_selection]{read_raven_selection()}} reads back,
so a write-then-read round trip returns the same ROIs (frequencies restored
to kHz).
}
\examples{
# Off-flow utility: export an ROI table to Raven format in tempdir.
\donttest{
rois <- data.frame(
  roi_start = 1, roi_end = 2, roi_min_freq = 2, roi_max_freq = 6,
  roi_label = "sp1", soundscape_file = "rec.wav")
f <- write_raven_selection(rois, file.path(tempdir(), "sel.txt"))
readLines(f, n = 2)   # header + first selection row
}
}
\seealso{
\code{\link[=read_raven_selection]{read_raven_selection()}}, \code{\link[=write_audacity_labels]{write_audacity_labels()}}
}
