% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_engines.R
\name{.score_fft}
\alias{.score_fft}
\title{\code{fft} engine — Lewis (1995) mean-centred NCC via FFT (RMF-01..05).}
\usage{
.score_fft(mat_soundscape, mat_template)
}
\arguments{
\item{mat_soundscape}{frames x bins.}

\item{mat_template}{frames x bins.}
}
\value{
numeric Pearson score per complete window.
}
\description{
Computes the \strong{same} Pearson score as \code{\link[=.score_cor]{.score_cor()}} but obtains
the cross term by FFT cross-correlation along the \strong{time axis only} (the
template band is fixed): \verb{cross = sum_b IFFT(FFT(s_b) * Conj(FFT(hpad_b)))},
reduced to one inverse FFT by summing the per-bin spectra first. This
dissolves the original's defects — no \code{colMeans} over frequency lags (RMF-01),
rigorous normalization via the shared mean-centred combine (RMF-02/04), a
single correlation convention (RMF-03) — and is band-aligned and
center-aligned by construction once the dispatcher pads it with the same
geometry as \code{cor} (RMF-05). Must reproduce \code{cor} within numerical tolerance.
}
