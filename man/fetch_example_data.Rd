% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_example_data.R
\name{fetch_example_data}
\alias{fetch_example_data}
\title{Download the monitoraSom example project into the local cache}
\usage{
fetch_example_data(overwrite = FALSE, timeout = 300, quiet = FALSE)
}
\arguments{
\item{overwrite}{Logical scalar. When \code{FALSE} (default) an existing cache is
kept and the download is skipped; when \code{TRUE} the bundle is re-downloaded
and the cached files are replaced.}

\item{timeout}{Positive numeric scalar: the download timeout in seconds
(default \code{300}). Applied for the duration of the call and restored
afterwards; the session-wide \code{getOption("timeout")} is never lowered.}

\item{quiet}{Logical scalar passed to \code{\link[utils:download.file]{utils::download.file()}}: when \code{FALSE}
(default) a progress bar is shown, when \code{TRUE} the download is silent.}
}
\value{
The path of the example project directory (a single character
string) on success, ready for \code{setwd()} or the \code{project_path} argument
of the pipeline functions; \code{NULL} on any failure. Called for its side
effect of populating the cache.
}
\description{
Fetches the runnable example project (the \code{basileuterus-culicivorus}
walkthrough: 14 WAVs, a ready \code{rois.duckdb}, the project marker and the
walkthrough in three formats) and unpacks it into a per-user cache. It is
\strong{opt-in}: you call it once, by hand; nothing in the package triggers a
download on its own.
}
\details{
The project is hosted as a single compressed bundle on a public release and
unpacked into \code{\link[tools:R_user_dir]{tools::R_user_dir()}}\verb{("monitoraSom", "cache")}, landing at
\verb{<cache>/basileuterus-culicivorus}. A second call does nothing unless
\code{overwrite = TRUE}: the function returns early when
\code{\link[=is_example_data_cached]{is_example_data_cached()}} is already \code{TRUE}.

Following CRAN's Internet-resource policy, the function \strong{fails gracefully}:
if the host is unreachable, the download times out, or the bundle is
malformed, it emits an informative \code{\link[=message]{message()}} and returns \code{NULL}
invisibly; it never errors or warns. Wrap dependent example code in
\verb{if (is_example_data_cached())} so a machine with no network still checks
cleanly.

The download URL is resolved through \code{getOption("monitoraSom.data_url", ...)},
so you can point the function at a mirror or a local copy without editing the
package: \code{options(monitoraSom.data_url = "file:///path/to/bundle.tar.gz")}.
}
\examples{
\dontrun{
# Fetch once; later calls are a no-op unless overwrite = TRUE.
example_project <- fetch_example_data()
setwd(example_project)  # or: launch_segmentation_app(project_path = ...)

# Point at a local bundle instead of the public release (offline testing):
options(monitoraSom.data_url = "file:///tmp/monitoraSom-example-bcu.tar.gz")
fetch_example_data(overwrite = TRUE)
}
}
\seealso{
\code{\link[=is_example_data_cached]{is_example_data_cached()}} to test the cache without downloading.
}
