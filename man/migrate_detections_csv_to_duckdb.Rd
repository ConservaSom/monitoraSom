% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detections_duckdb.R
\name{migrate_detections_csv_to_duckdb}
\alias{migrate_detections_csv_to_duckdb}
\title{Migrate a legacy detections CSV into a DuckDB detections store (one-shot
converter)}
\usage{
migrate_detections_csv_to_duckdb(csv_path, duckdb_path, overwrite = FALSE)
}
\arguments{
\item{csv_path}{path to a legacy detections CSV.}

\item{duckdb_path}{destination \code{.duckdb} file.}

\item{overwrite}{overwrite an existing destination. Default \code{FALSE}.}
}
\value{
invisibly, the migrated data.frame.
}
\description{
Store-level utility for the detections store, exported for
advanced use (not part of the everyday pipeline). Reads a legacy detections
CSV (21-column original layout or
full 26-column standard layout) and persists it into a DuckDB detections
store with upsert semantics. Complements \code{\link[=migrate_metadata_csv_to_duckdb]{migrate_metadata_csv_to_duckdb()}}
and \code{\link[=migrate_templates_to_db]{migrate_templates_to_db()}} (G3 in plan-log-2026-06-22-001).

The CSV is coerced to the standard 26-column schema via
\code{.coerce_detections()}: missing standard columns become typed \code{NA}, extra
columns are dropped. Rows are upserted by \code{detection_id} so re-running this
on the same CSV is idempotent. When the CSV carries no \code{detection_id}
column (legacy 21-col layout), a stable id is synthesized per row from the
durable key via \code{.detection_id()} (substituting \code{template_file} for the
absent \code{template_id}), so legacy rows insert cleanly under the NOT NULL
primary key and re-running upserts the same ids (CRAN-98).
}
\keyword{internal}
