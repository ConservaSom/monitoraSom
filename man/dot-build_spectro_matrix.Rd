% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_helpers.R
\name{.build_spectro_matrix}
\alias{.build_spectro_matrix}
\title{Build one spectrogram matrix (frames x frequency-bins) + its time vector}
\usage{
.build_spectro_matrix(wav_path, spec_params, tlim = NULL, which_file = "file")
}
\arguments{
\item{wav_path}{WAV file.}

\item{spec_params}{\code{\link[=.spec_params_from_row]{.spec_params_from_row()}} output.}

\item{tlim}{optional \code{c(start, end)} (templates only).}

\item{which_file}{label for error messages.}
}
\value{
list(\code{mat} = \code{t(amp)} frames x bins, \code{time} = frame times).
}
\description{
The reusable building block the dispatcher and the batch
orchestrator share, so a soundscape spectrogram can be computed \strong{once per
group} and reused across that group's templates (RMC-10 cache realised at
the batch level). Validates \code{flim} against the file's Nyquist (RMC-11).
}
