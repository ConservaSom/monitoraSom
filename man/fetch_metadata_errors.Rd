% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_errors.R
\name{fetch_metadata_errors}
\alias{fetch_metadata_errors}
\title{Read the metadata error log back (DEC-25/STEP-20c)}
\usage{
fetch_metadata_errors(errors_source = NULL)
}
\arguments{
\item{errors_source}{Where the log lives. A path to a metadata cache
(\code{.duckdb} or \code{.csv}) whose sibling/embedded log is read, or the path of
an explicit error-log file (\code{.csv}). \code{NULL} (default) uses the standard
cache \code{soundscapes/soundscapes_metadata.duckdb} when it exists and stops
with an actionable message otherwise.}
}
\value{
A data.frame with the four standard columns; zero rows when the log
is absent or empty.
}
\description{
Returns the per-file error log produced by
\code{\link[=fetch_soundscape_metadata]{fetch_soundscape_metadata()}} in the standard four-column form
(\code{soundscape_path}, \code{error_class}, \code{error_message}, \code{timestamp}). The
reader follows the backend: a \code{.duckdb} cache reads the \code{metadata_errors}
table; a \code{.csv} cache reads the sibling \verb{<base>_errors.csv}; an explicit
error-log path is read directly.
}
\examples{
\donttest{
# Scan a folder that holds one broken file, then read the log back
# without SQL:
demo <- file.path(tempdir(), "err_demo"); dir.create(demo, showWarnings = FALSE)
writeLines("not a wav", file.path(demo, "broken.wav"))
synth <- function(dur_s, name) {
  sr <- 16000
  w <- tuneR::normalize(tuneR::sine(4000, duration = dur_s * sr,
                                    samp.rate = sr), unit = "16")
  tuneR::writeWave(w, file.path(demo, name))
}
synth(2, "good_01.wav"); synth(2, "good_02.wav")
cache <- file.path(tempdir(), "err_demo.duckdb")
fetch_soundscape_metadata(demo, output_file = cache, on_error = "warn")
err <- fetch_metadata_errors(cache)
head(err)
}
}
\seealso{
\code{\link[=fetch_soundscape_metadata]{fetch_soundscape_metadata()}}
}
