% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_workspace.R
\name{set_workspace}
\alias{set_workspace}
\title{Set up a monitoraSom project workspace}
\usage{
set_workspace(
  project_path = NULL,
  example_data = FALSE,
  app_presets_path = "app_presets",
  templates_path = "templates",
  match_scores_path = NA_character_,
  soundscapes_path = NA_character_,
  recordings_path = NA_character_,
  roi_tables_path = NA_character_,
  roi_cuts_path = NA_character_,
  templates_metadata_path = NA_character_,
  grids_path = NA_character_,
  detections_path = NA_character_,
  detection_cuts_path = NA_character_,
  detection_spectrograms_path = NA_character_,
  validations_path = NA_character_,
  diagnostics_path = NA_character_
)
}
\arguments{
\item{project_path}{Path to an existing, writable project directory; the whole
workspace is created beneath it. \strong{Required}, as there is no default and no
auto-detection. A missing or non-writable directory errors immediately.}

\item{example_data}{Discontinued. The runnable example ships as a standalone
project: call \code{\link[=fetch_example_data]{fetch_example_data()}} and use the directory it returns as
\code{project_path}. When \code{TRUE}, a message points to that flow and nothing is
populated. Kept only so existing scripts get a readable message.}

\item{app_presets_path, templates_path, match_scores_path, soundscapes_path}{Each is a single character string: the \strong{bare directory name} (relative to
\code{project_path}) for that workspace subdirectory. Pass an absolute path to
place the directory elsewhere, or a single \code{NA} to skip its creation. Only
the essential pair (\code{templates}, \code{app_presets}) defaults to a name;
\code{match_scores} defaults to \code{NA} (created on demand when scores are
persisted); every other directory defaults to \code{NA} (created on demand by the
function that uses it). \code{grids}, \code{validations} and \code{diagnostics} are the
names used since monitoraSom 1.2.0 (before 1.2.0 they were
\code{match_grid_metadata}, \code{validation_outputs} and
\code{validation_diagnostics}).}

\item{recordings_path, roi_tables_path, roi_cuts_path, templates_metadata_path}{Workspace subdirectory name; same rules as \code{app_presets_path}: a bare
directory name, an absolute path, or \code{NA} (default) to skip creation.}

\item{grids_path, detections_path, detection_cuts_path, detection_spectrograms_path}{Workspace subdirectory name; same rules as \code{app_presets_path}: a bare
directory name, an absolute path, or \code{NA} (default) to skip creation.}

\item{validations_path, diagnostics_path}{Workspace subdirectory name; same
rules as \code{app_presets_path}: a bare directory name, an absolute path, or
\code{NA} (default) to skip creation.}
}
\value{
Invisibly, a named logical vector with \strong{one element per non-skipped
directory} (an \code{NA}-skipped directory is absent from the vector, not
\code{FALSE}): \code{TRUE} if the directory was created this call, \code{FALSE} if it
already existed. Directories, the marker and presets are written as side
effects.
}
\description{
Creates a monitoraSom project on disk. It lays out the standard folder
skeleton under \code{project_path}, writes a plain-text project marker
(\code{monitoraSom.proj}) and creates the editable \verb{app_presets/} default files.
It is the first function a new user runs
and produces the folder layout every later step reads from and writes into.
}
\details{
Run this once, in an \strong{existing, writable} directory that will hold the
project. The function is deliberately simple and stable: it only lays out
directories and a couple of small text/preset files. It does \strong{not} create
the pipeline databases: each \code{.duckdb} database is created when the function
that owns it writes to it for the first time (the segmentation app writes
\code{rois.duckdb}, \code{export_templates()} writes \code{templates/templates.duckdb}, and
so on), so a fresh workspace holds folders, not databases.

The default skeleton creates only the essential folders: \verb{templates/} (ROI
cuts + template database) and \verb{app_presets/} (customisation), plus the
project marker. \verb{match_scores/} is created on demand by \code{\link[=run_matching]{run_matching()}}
when scores are persisted (DEC-3).
Every other folder is created \strong{on demand} by the function that needs it,
and is skipped here unless you pass its path explicitly (\code{NA}, the default
for all of them). \code{rois.duckdb} stays at the project root.
Each \verb{*_path} argument takes a \strong{bare directory name} placed under
\code{project_path}. Pass an absolute path to put that directory elsewhere, or
\code{NA} to skip it entirely (useful when, for example, your soundscapes already
live in an external folder). For a ready-made runnable example project, see
\code{\link[=fetch_example_data]{fetch_example_data()}}; \code{example_data} no longer populates the workspace.

Two things to know: \code{project_path} is \strong{required} (there is no
auto-detection), and the directory must already exist and be writable, or
the call stops right away. Nothing is ever silently overwritten: the marker
and presets are written only when missing. The remaining
folders (scores, detections, validations, diagnostics) are created later, by
the functions that fill them, not by this function.
}
\section{Pipeline context}{

Step 0 of the monitoraSom analysis flow. Reads nothing (entry point).
Produces the workspace directory layout and project marker
used by every later step. See also: \code{\link{launch_segmentation_app}}.
}

\examples{
\dontrun{
# Load the package
library(monitoraSom)

# Step 0: create a monitoraSom project workspace in a temporary directory.
proj <- file.path(tempdir(), "my_monitoring_project")
dir.create(proj)

# Check the content
list.dirs(proj)
list.files(proj)

# Skeleton (directories + marker):
set_workspace(project_path = proj)

# Check the content again
list.dirs(proj)
list.files(proj)
}
}
\seealso{
\code{\link{launch_segmentation_app}} (the next step: segment
soundscapes into ROIs).
}
