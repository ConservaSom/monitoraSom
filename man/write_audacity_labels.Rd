% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_annotations.R
\name{write_audacity_labels}
\alias{write_audacity_labels}
\title{Write ROI data as an Audacity label track}
\usage{
write_audacity_labels(rois, path)
}
\arguments{
\item{rois}{A data.frame of ROIs. Required columns: \code{roi_start}, \code{roi_end}
(seconds) and \code{roi_label}; optional \code{roi_min_freq}/\code{roi_max_freq} (kHz,
written as Hz) add the frequency band. Any \code{\link[=fetch_rois]{fetch_rois()}} output qualifies.}

\item{path}{Output file path for the Audacity label track.}
}
\value{
\code{path}, invisibly (the function is called for its file side effect).
}
\description{
Exports a monitoraSom ROI table to an Audacity label
track, so ROIs can be opened and edited in Audacity. Frequencies are
converted from kHz back to Hz. The inverse of \code{\link[=read_audacity_labels]{read_audacity_labels()}}.
}
\details{
Each ROI is written as \verb{start<TAB>end<TAB>label}; rows carrying a frequency
band add the \verb{\\<TAB>lowHz<TAB>highHz} continuation line (Audacity's extended
format), while rows with \code{NA} frequencies omit it. This is exactly the layout
\code{\link[=read_audacity_labels]{read_audacity_labels()}} parses, so a write-then-read round trip returns the
same ROIs (frequencies restored to kHz).
}
\examples{
# Off-flow utility: export an ROI table to Audacity format in tempdir.
\donttest{
rois <- data.frame(
  roi_start = 1, roi_end = 2, roi_min_freq = 2, roi_max_freq = 6,
  roi_label = "sp1", soundscape_file = "rec.wav")
f <- write_audacity_labels(rois, file.path(tempdir(), "labels.txt"))
readLines(f)   # label line + frequency continuation line
}
}
\seealso{
\code{\link[=read_audacity_labels]{read_audacity_labels()}}, \code{\link[=write_raven_selection]{write_raven_selection()}}
}
