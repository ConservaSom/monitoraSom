% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_matching_helpers.R
\name{.pad_score_to_length}
\alias{.pad_score_to_length}
\title{Pad the per-window score vector to soundscape length (RMC-01/02/03)}
\usage{
.pad_score_to_length(score, sliding_window, n_time, sentinel = 0)
}
\arguments{
\item{score}{numeric, one value per complete sliding window.}

\item{sliding_window}{integer template frame count.}

\item{n_time}{integer length of the soundscape time vector.}

\item{sentinel}{numeric border / NA fill (default \code{0}).}
}
\value{
numeric of length \code{n_time}, frame-aligned to the soundscape.
}
\description{
Corrected, \strong{symmetric-by-geometry} padding: \code{pad_left = sw_start}, \code{pad_right = sw_end} (RMC-01 — fixes the original \code{sw_start - 1} /
\code{sw_end + 1} one-frame left shift). The total length is exactly
\code{length(soundscape_time)}, asserted as a hard invariant (RMC-02, replacing
the \code{head(score, -1)} band-aid). Borders and residual \code{NA}s are filled with
the principled sentinel \code{0} (RMC-03 — "no linear association" for Pearson;
pair-independent and uniform across the three versions), not the per-pair
minimum.
}
