% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_annotations.R
\name{import_annotations}
\alias{import_annotations}
\title{Read and write Raven & Audacity annotations as ROI data}
\description{
Convert between two common manual annotation formats, Raven
selection tables or Audacity label tracks, and the monitoraSom standard
ROI format. The four functions
(\code{\link[=read_raven_selection]{read_raven_selection()}}, \code{\link[=read_audacity_labels]{read_audacity_labels()}},
\code{\link[=write_raven_selection]{write_raven_selection()}}, \code{\link[=write_audacity_labels]{write_audacity_labels()}}) let you bring
annotations into the package as ROI tables and export ROI tables back to
either format.
}
\details{
Both formats hold \strong{manual annotations/selections} (ground truth), so they
map to ROI tables (\code{roi_tables}), never to detections, because a detection
needs a \code{peak_score} that these formats do not carry. Use these functions
to prepare ground truth for \code{\link[=validate_by_overlap]{validate_by_overlap()}} from an existing
Raven/Audacity workflow, or to round trip ROIs out for editing in those
tools. Two conversions happen
automatically and are the usual source of confusion: frequency is stored in
\strong{Hz} by Raven/Audacity but in \strong{kHz} by the ROI format (\code{roi_min_freq}/
\code{roi_max_freq}), so it is divided by 1000 on read and multiplied on write; and
every imported row records where it came from in \code{roi_source}
(\code{"import_raven"} for Raven tables, \code{"import_audacity"} for Audacity
label tracks; the \code{signals} store's \code{origin}
column carries them through unchanged). Both formats are simple
tab-separated text, so reading and writing use base R only and add no
dependency to the package.
}
\section{Pipeline context}{

A utility used across the flow, outside the numbered steps. Reads and writes
Raven \code{.txt} selection tables or Audacity label tracks on one side and
ROI data on the other; the ROI side feeds ground-truth-based
steps such as \code{\link[=validate_by_overlap]{validate_by_overlap()}}.
}

\seealso{
\code{\link[=fetch_rois]{fetch_rois()}}, \code{\link[=validate_by_overlap]{validate_by_overlap()}}
}
