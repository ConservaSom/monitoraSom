% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_rois.R
\name{fetch_rois}
\alias{fetch_rois}
\title{Gather ROI tables into a \code{df_rois} data.frame}
\usage{
fetch_rois(
  rois_path = NULL,
  recursive = FALSE,
  source = c("duckdb", "csv"),
  soundscape_metadata = NULL,
  roi_source = NULL,
  include_detections = FALSE
)
}
\arguments{
\item{rois_path}{Where to read ROIs from. \code{NULL} (default) reads the
standard ROI database \code{rois.duckdb} in the current working directory (the
project root). For \code{source = "duckdb"}: a single ROI \code{.duckdb} file \strong{or}
a directory holding one or more of them, all read into one table. For
\code{source = "csv"}: a directory of \verb{*_roi_*.csv} tables from versions
before 1.2.0. Must be a
single, existing, non-\code{NA} character path.}

\item{recursive}{Logical. When \code{TRUE}, search subdirectories of \code{rois_path}
too. Default \code{FALSE} (top level only). Useful to read ROIs spread across
per-site/per-session subfolders.}

\item{source}{Which reader to use: \code{"duckdb"} (the routine app output,
default) or \code{"csv"} (migration of pre-1.2.0 exports). There is no
autodetection.}

\item{soundscape_metadata}{Optional source of origin information: a
\code{df_soundscapes} data.frame \strong{or} a path to a soundscape-metadata
\code{.duckdb}. When supplied,
\code{origin_}-prefixed columns (\code{origin_soundscape_path}/\verb{_file}/\verb{_sha256})
are joined onto \code{df_rois} by \code{soundscape_path}. \code{NULL} (default) skips
enrichment. Requires the metadata to carry \code{soundscape_path},
\code{soundscape_file} and \code{soundscape_sha256}.}

\item{roi_source}{Optional provenance filter. \code{NULL} (default) returns every
ROI; \code{"manual"} keeps only ground-truth ROIs (manual/unstamped, never
detection-derived); \code{"detection"} keeps only detection-derived ROIs: the
output of \code{\link[=detecs_to_rois]{detecs_to_rois()}}, the \code{detection_to_roi} rows promoted by
\code{\link[=validate_by_overlap]{validate_by_overlap()}}, and, when \code{include_detections = TRUE}, the
detection rows themselves.}

\item{include_detections}{Logical, default \code{FALSE}. When \code{TRUE}, projection
also covers the \code{"detection"} and \code{"detection_to_roi"} rows of a unified
signals store, so promoted rows and raw detections read back in ROI form
(DEC-26/STEP-20d). Legacy \code{rois}-table stores are unaffected.}
}
\value{
A data.frame of ROIs in the standard format, one row per ROI,
optionally with the three \verb{origin_*} provenance columns added when
\code{soundscape_metadata} is supplied. Attribute \code{spectro_summary} carries
the distinct \code{roi_wl}/\code{roi_ovlp}/\code{roi_sample_rate}/\code{roi_pitch_shift}
tuples with row counts (LSA-206): ROI bounds are only comparable across
ROIs segmented under the same parameters; a \code{message()} flags
heterogeneous inputs.
}
\description{
Reads the regions of interest (ROIs) produced by the segmentation app and
returns them as one data.frame (\code{df_rois}) in the standard ROI format: this
is pure ROI data, with no detections in it yet. It
reads one ROI \code{.duckdb} database, or many, into one table. The usual next
step is to split \code{df_rois} into a template branch (for \code{\link[=export_templates]{export_templates()}})
and a soundscape branch (for validation).
}
\details{
The routine path is \code{source = "duckdb"}. This is the format the
segmentation app has written since monitoraSom 1.2.0. Point \code{rois_path} at a
single \code{.duckdb} file, or at a directory to read every ROI database found in
it (set \code{recursive = TRUE} to also search subfolders). Use \code{source = "csv"}
only to migrate old \verb{*_roi_*.csv} exports; the output format is identical
either way.

\code{fetch_rois()} does \strong{not} decide which ROIs are templates and which are
search soundscapes. That split is yours, usually a \code{dplyr::filter()} on
\code{soundscape_path}. Two optional refinements: \code{roi_source} keeps only
ground-truth ROIs or only detection-derived ROIs, and \code{soundscape_metadata}
adds \verb{origin_*} columns that record where each ROI's source recording came
from, so later steps can trace it back.

Two things to know: the returned frame is always the standard 18-column
format (\code{roi_channel} present; the \code{roi_path}/\code{roi_file} columns from
versions before 1.2.0 are gone), so code reading those columns must
adapt; and when \code{soundscape_metadata} is supplied, ROIs whose recording
is absent from it get \code{NA} provenance and a warning rather than a hard
error.
}
\section{Pipeline context}{

Step 2 of the monitoraSom analysis flow. Reads the ROI \code{.duckdb} database
written by \code{\link{launch_segmentation_app}} (step 1). Returns a
\code{df_rois} data.frame used by \code{\link{export_templates}} (step 3, the
template branch) and by \code{\link{validate_by_overlap}} (the validation
branch).
}

\examples{
\donttest{
# Step 2: read the segmentation app's DuckDB ROI database into one table.
# Load the package
library(monitoraSom)
rois_db <- system.file("extdata", "template_trio", "rois.duckdb",
                       package = "monitoraSom")
df_rois <- fetch_rois(rois_path = rois_db)
str(df_rois)          # standard 18-column ROI format, one row per ROI

# Variation: read every ROI database found under a directory (e.g. per-site):
rois_dir <- system.file("extdata", "template_trio", package = "monitoraSom")
df_all <- fetch_rois(rois_path = rois_dir, recursive = TRUE)
str(df_all)
}
}
\seealso{
\code{\link{launch_segmentation_app}} (the previous step that writes
the ROI database); \code{\link{export_templates}} (the next step that
turns the template branch into templates).
}
