% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_signals.R
\name{.signals_as_detections}
\alias{.signals_as_detections}
\title{Signals -> legacy \code{detections} shape (27 cols). Keeps \code{signal_class == "detection"} only; other classes are dropped with an informative message
(they carry no det_* data — keeping them would fabricate values).}
\usage{
.signals_as_detections(signals)
}
\arguments{
\item{signals}{Standard signals frame (data frame).}
}
\description{
Signals -> legacy \code{detections} shape (27 cols). Keeps \code{signal_class == "detection"} only; other classes are dropped with an informative message
(they carry no det_* data — keeping them would fabricate values).
}
