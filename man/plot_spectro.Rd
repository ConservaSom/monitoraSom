% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_spectro.R
\name{plot_spectro}
\alias{plot_spectro}
\title{Render a SpectroResult into a ggplot spectrogram}
\usage{
plot_spectro(
  spectro,
  dyn_range = c(-100, -20),
  color_scale = "inferno",
  n_colors = 124,
  interpolate = FALSE,
  invert_colormap = FALSE,
  theme_mode = "light",
  time_guide_interval = 3,
  freq_guide_interval = 1,
  font_scale = 1
)
}
\arguments{
\item{spectro}{A \code{"SpectroResult"} from \code{\link[=extract_spectro]{extract_spectro()}}.}

\item{dyn_range}{A numeric vector of length 2 giving the minimum and maximum
relative amplitude (dB) to display.}

\item{color_scale}{A character colour scale: \code{"viridis"}, \code{"magma"},
\code{"inferno"}, \code{"cividis"}, \code{"greyscale 1"} or \code{"greyscale 2"}.}

\item{n_colors}{An integer number of colours in the scale. Smaller values
render faster at lower colour resolution.}

\item{interpolate}{A logical passed to \code{annotation_raster()}.}

\item{invert_colormap}{A logical; if \code{TRUE}, reverse the colour scale so
quiet regions take the "high" colour.}

\item{theme_mode}{A character theme: \code{"dark"} or \code{"light"}.}

\item{time_guide_interval}{A numeric interval between time guides (seconds).
\code{0} disables time guides (no layer is drawn).}

\item{freq_guide_interval}{A numeric interval between frequency guides (kHz).
\code{0} disables frequency guides (no layer is drawn).}

\item{font_scale}{A single positive numeric multiplier applied to every text
element in the plot (axis text/titles and legend text/title). \code{1} (default)
reproduces the default sizes; values \verb{> 1} enlarge and \verb{< 1} shrink all
spectrogram text together.}
}
\value{
A \code{ggplot2} object.
}
\description{
The rendering half of \code{\link[=fast_spectro]{fast_spectro()}}: it turns a
\code{SpectroResult} (from \code{\link[=extract_spectro]{extract_spectro()}}) into a \code{ggplot2} spectrogram.
Rendering is roughly ten times faster than \code{geom_raster()}/\code{geom_tile()}
because the dB matrix is drawn as a pre-encoded native raster via
\code{annotation_raster()} (technique credited to Sergio Oller,
\url{https://github.com/tidyverse/ggplot2/issues/4989}).
}
\details{
Call this when you have already computed the data with \code{\link[=extract_spectro]{extract_spectro()}}
and want to draw it, possibly several times with different display
settings
(colour scale, dynamic range, theme) without recomputing the FFT. \code{dyn_range}
sets the dB window mapped onto the colour scale: values below \code{dyn_range[1]}
render as the "low" colour, so raise its floor to suppress background noise
or lower it to reveal faint structure. Time and frequency guide lines are
drawn at fixed intervals; set either interval to \code{0} to omit that set of
guides. Note: the input must be a \code{SpectroResult}; passing a raw matrix or
a \code{Wave} errors; use \code{\link[=fast_spectro]{fast_spectro()}} to go straight from a recording.
}
\section{Pipeline context}{

A utility used across the flow, outside the numbered steps. Reads a
\code{SpectroResult} from \code{\link[=extract_spectro]{extract_spectro()}}. Produces a \code{ggplot2} spectrogram
for display or further annotation.
}

\examples{
# Off-flow utility: render a precomputed SpectroResult, re-themed cheaply.
\dontrun{
# Load the package
library(monitoraSom)
# (The package ships no field recordings; synthesize one to draw.)
wav <- file.path(tempdir(), "rec_01.wav")
rec <- tuneR::normalize(tuneR::sine(4000, duration = 3 * 16000,
                                    samp.rate = 16000), unit = "16")
tuneR::writeWave(rec, wav)
sp  <- extract_spectro(tuneR::readWave(wav), flim = c(0, 10))

p <- plot_spectro(sp, color_scale = "inferno")
class(p)   # "gg" / "ggplot"

# Same data, dark theme + a tighter dynamic range:
plot_spectro(sp, theme_mode = "dark", dyn_range = c(-80, -20))
}
}
\seealso{
\code{\link[=extract_spectro]{extract_spectro()}}, \code{\link[=fast_spectro]{fast_spectro()}}
}
