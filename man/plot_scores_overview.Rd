% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scores.R
\name{plot_scores_overview}
\alias{plot_scores_overview}
\title{Plot a detection-count heatmap for a whole matching run}
\usage{
plot_scores_overview(
  detections,
  label = c("template_label", "template_name"),
  low_color = "#3B0F70",
  high_color = "#FDE725",
  show_counts = TRUE
)
}
\arguments{
\item{detections}{A detections data.frame as returned by \code{\link[=run_matching]{run_matching()}} /
\code{\link[=template_matching]{template_matching()}} (standard detections format). Must carry
\code{soundscape_file} and at least one of \code{template_label} / \code{template_name}.}

\item{label}{Which template column to show on the y-axis: \code{"template_label"}
(default, the species/label) or \code{"template_name"}. Falls back to the other
column when the chosen one is absent or all-\code{NA}.}

\item{low_color, high_color}{End colours of the count fill gradient. Defaults
are the viridis dark/bright endpoints; change to recolour the scale.}

\item{show_counts}{Logical, default \code{TRUE}. Prints the per-cell count on each
tile; set \code{FALSE} for a colour-only heatmap on large grids.}
}
\value{
A \code{ggplot} heatmap with one tile per soundscape-template pair, the
fill and (optionally) the printed number giving the detection count.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Summarises an entire matching run at a glance. Where \code{\link[=plot_scores]{plot_scores()}} shows
one soundscape-template pair, this draws a \code{ggplot2} heatmap of the whole
grid: one tile per (template, soundscape) pair, filled and labelled by the
number of detections in that pair. It answers "which templates fired, on
which recordings, and how much" in a single figure.
}
\details{
Counts come from \code{table()} over the detections, which completes the full
soundscape-template grid: a pair that never produced a detection renders as
a \code{0} tile, so silent gaps are as visible as hits. This complements the
tabular \code{\link[=summarise_matching]{summarise_matching()}}. Use it right after a run to spot templates
that never fire or recordings that no template matched. Note: the y-axis
uses \code{template_label} by default and silently falls back to \code{template_name}
when the label column is absent or all-\code{NA}, so a run without labels still
plots (keyed by name).
}
\section{Pipeline context}{

Step 13 of the monitoraSom analysis flow (visualization). Reads the
detections table from \code{\link[=run_matching]{run_matching()}} / \code{\link[=template_matching]{template_matching()}} (step 7).
Produces a \code{ggplot} heatmap for human inspection, a leaf of the flow.
}

\examples{
# Step 13 of the analysis flow: overview a whole matching run (step 7) as a
# per-pair detection-count heatmap.
\dontrun{
# Build a tiny run from synthesized recordings (see [fetch_match_grid()] for
# the full recipe), then overview the whole run as a per-pair count heatmap.
rec_dir <- file.path(tempdir(), "recs"); dir.create(rec_dir, showWarnings = FALSE)
# Two soundscapes: one clear 4 kHz burst, one with a fainter copy, so the
# two tiles show different detection counts.
sr <- 16000
set.seed(7)
tt <- seq_len(10 * sr) / sr
x1 <- rnorm(length(tt), 0, 0.02)
x1[(tt >= 1) & (tt < 1.4)] <- x1[(tt >= 1) & (tt < 1.4)] +
  0.6 * sin(2 * pi * 4000 * tt[(tt >= 1) & (tt < 1.4)])
rec1 <- tuneR::normalize(tuneR::Wave(x1, samp.rate = sr, bit = 16), unit = "16")
tuneR::writeWave(rec1, file.path(rec_dir, "siteA_01.wav"))
x2 <- rnorm(length(tt), 0, 0.02)
x2[(tt >= 2) & (tt < 2.4)] <- x2[(tt >= 2) & (tt < 2.4)] +
  0.25 * sin(2 * pi * 4000 * tt[(tt >= 2) & (tt < 2.4)])
rec2 <- tuneR::normalize(tuneR::Wave(x2, samp.rate = sr, bit = 16), unit = "16")
tuneR::writeWave(rec2, file.path(rec_dir, "siteA_02.wav"))
df_rois <- data.frame(
  soundscape_path = file.path(rec_dir, "siteA_01.wav"),
  soundscape_file = "siteA_01.wav",
  roi_label = "burst", roi_start = 0.8, roi_end = 1.2,
  roi_min_freq = 3, roi_max_freq = 5, roi_wl = 512, roi_ovlp = 50,
  stringsAsFactors = FALSE)
out_dir <- file.path(tempdir(), "templates")
export_templates(df_rois, templates_path = out_dir, create_dir = TRUE)
det <- template_matching(
  fetch_match_grid(fetch_soundscape_metadata(rec_dir),
                   fetch_template_metadata(out_dir)),
  output = "detections")

plot_scores_overview(det)                        # counts printed on the tiles
plot_scores_overview(det, show_counts = FALSE)   # colour-only heatmap
}
}
\seealso{
\code{\link[=plot_scores]{plot_scores()}}, \code{\link[=run_matching]{run_matching()}}, \code{\link[=template_matching]{template_matching()}},
\code{\link[=summarise_matching]{summarise_matching()}}
}
