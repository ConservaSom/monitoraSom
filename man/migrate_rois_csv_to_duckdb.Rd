% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roi_duckdb.R
\name{migrate_rois_csv_to_duckdb}
\alias{migrate_rois_csv_to_duckdb}
\title{Migrate legacy CSV ROI files into a DuckDB ROI store (one-shot converter)}
\usage{
migrate_rois_csv_to_duckdb(
  csv_path,
  duckdb_path,
  overwrite = FALSE,
  recursive = FALSE,
  workspace_root = NULL
)
}
\arguments{
\item{csv_path}{path to a legacy ROI CSV file or a directory of per-soundscape
ROI CSV files.}

\item{duckdb_path}{destination \code{.duckdb} file.}

\item{overwrite}{overwrite an existing destination. Default \code{FALSE}.}

\item{recursive}{scan subdirectories when \code{csv_path} is a directory.
Default \code{FALSE}.}

\item{workspace_root}{optional workspace root (CRAN-103): legacy absolute
\code{soundscape_path} values under it are rewritten workspace-relative on
migration; absolute paths outside it are kept. Absolute paths elsewhere
in a store can be re-canonicalized by re-running this migrator over an
export produced by \code{\link[=export_rois_duckdb_to_csv]{export_rois_duckdb_to_csv()}}.}
}
\value{
invisibly, the migrated data.frame.
}
\description{
Store-level utility for the ROI store, exported for
advanced use (not part of the everyday pipeline). Reads a directory of
legacy per-soundscape \verb{*_roi_*.csv} files
produced by the original pipeline (or a single legacy CSV) and persists
them into a DuckDB ROI store. Complements \code{\link[=migrate_templates_to_db]{migrate_templates_to_db()}}
and \code{\link[=migrate_metadata_csv_to_duckdb]{migrate_metadata_csv_to_duckdb()}} (G4 in plan-log-2026-06-22-001).

When \code{csv_path} is a directory, it is scanned recursively for CSV files
matching the \verb{*_roi_*.csv} pattern (the legacy naming convention). When it
is a single file, that file is read as a combined ROI table. Rows are
coerced to the standard ROI schema via \code{.coerce_rois()} and upserted by
soundscape_path.
}
\keyword{internal}
