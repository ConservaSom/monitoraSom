% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_template_metadata.R
\name{catalog_templates}
\alias{catalog_templates}
\title{Summarise a template set as a table (and optional contact sheet)}
\usage{
catalog_templates(
  templates_path,
  output = NULL,
  thumbnails = !is.null(output),
  ncol = 4,
  soundscapes_dir = NULL,
  ...
)
}
\arguments{
\item{templates_path}{A template database \code{.duckdb} path.}

\item{output}{Optional path for a PNG contact sheet. When \code{NULL} (default) no
image is written and only the table is returned.}

\item{thumbnails}{Logical. Render spectrogram thumbnails into the contact
sheet. Defaults to \code{TRUE} when \code{output} is set, \code{FALSE} otherwise; only has
an effect together with \code{output}.}

\item{ncol}{Integer, default \code{4}. Thumbnails per row in the contact sheet.}

\item{soundscapes_dir}{Optional fallback directory of source recordings, used
to locate the source for \code{reference_metadata} thumbnails when their recorded
\code{origin_soundscape_path} is not reachable.}

\item{...}{Passed through to \code{\link[=fast_spectro]{fast_spectro()}} (e.g. window length, overlap).}
}
\value{
Invisibly, the catalog table (a subset of the standard template
format), one row per template.
}
\description{
Produces a scannable overview of a template database \strong{without
opening each audio file}: a compact metadata table and, optionally, a
spectrogram contact-sheet PNG with one thumbnail per template. A leaf
reporting utility for inspecting a template set before matching; nothing
downstream depends on its output.
}
\details{
The returned table is a subset of the standard format (label, mode, bounds,
band, sample rate, hash), enough to eyeball a template set at a glance.
The
optional PNG renders each template with \code{\link[=fast_spectro]{fast_spectro()}}: \code{standalone_audio}
templates show their cut, \code{reference_metadata} templates are cut on demand
from the source recording. Templates whose audio cannot be located draw a
placeholder panel rather than failing the whole sheet.

Use it as a sanity check after \code{\link[=export_templates]{export_templates()}} or a migration. Things to
know:
(1) \code{thumbnails} defaults to \code{TRUE} only when \code{output} is set; pass an
\code{output} path to get an image at all. (2) Rendering \code{reference_metadata}
thumbnails needs the source recordings reachable; supply \code{soundscapes_dir}
if their recorded \code{origin_soundscape_path} no longer resolves.
}
\section{Pipeline context}{

Off-flow inspection utility for step 4. Reads a template database
\code{.duckdb} (as \code{\link[=fetch_template_metadata]{fetch_template_metadata()}} does). Produces a summary table
and, optionally, a PNG contact sheet; no pipeline object. See also:
\code{\link{fetch_template_metadata}}.
}

\examples{
\dontrun{
# Inspect a template set at a glance (table only, no audio opened).
wav <- file.path(tempdir(), "rec_01.wav")
rec <- tuneR::normalize(tuneR::sine(4000, duration = 3 * 16000,
                                    samp.rate = 16000), unit = "16")
tuneR::writeWave(rec, wav)
df_rois <- data.frame(
  soundscape_path = wav, soundscape_file = "rec_01.wav",
  roi_label = "burst", roi_start = 0.5, roi_end = 1.0,
  roi_min_freq = 2, roi_max_freq = 6, roi_wl = 512, roi_ovlp = 50,
  stringsAsFactors = FALSE)
out_dir <- file.path(tempdir(), "templates")
export_templates(df_rois, templates_path = out_dir, create_dir = TRUE)
catalog <- catalog_templates(file.path(out_dir, "templates.duckdb"))
catalog[, c("template_label", "template_mode", "template_min_freq",
            "template_max_freq")]

# Also write a spectrogram contact sheet:
catalog_templates(file.path(trio, "templates.duckdb"),
                  output = file.path(tempdir(), "templates.png"))
}
}
\seealso{
\code{\link[=fetch_template_metadata]{fetch_template_metadata()}}, \code{\link[=fast_spectro]{fast_spectro()}}.
}
