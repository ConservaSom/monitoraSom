% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_duckdb.R
\name{migrate_metadata_csv_to_duckdb}
\alias{migrate_metadata_csv_to_duckdb}
\title{Migrate a legacy CSV metadata cache into a DuckDB database (FSM-210)}
\usage{
migrate_metadata_csv_to_duckdb(csv_path, duckdb_path, overwrite = FALSE)
}
\arguments{
\item{csv_path}{path to the legacy CSV cache.}

\item{duckdb_path}{destination \code{.duckdb} file.}

\item{overwrite}{overwrite an existing destination. Default \code{FALSE}.}
}
\value{
invisibly, the migrated data.frame.
}
\description{
Store-level utility for the metadata store, exported for
advanced use (not part of the everyday pipeline).

Reads the old CSV, renames \code{soundscape_bitrate} -> \code{soundscape_bitdepth}
(FSM-13), fills missing standard columns with \code{NA}, and upserts into
DuckDB. Newly introduced columns left \code{NA} are reported with a warning
instructing the user to re-run \code{\link[=fetch_soundscape_metadata]{fetch_soundscape_metadata()}} to populate
them from the recordings (FSM-210 user decision).

\strong{Known limitation (G6 in plan-log-2026-06-22-001):} This migration does
not re-read the original WAV files, so \code{soundscape_sha256} stays \code{NA} for
all migrated rows. Re-run \code{\link[=fetch_soundscape_metadata]{fetch_soundscape_metadata()}} after migration to
populate the integrity hashes from the recordings. A "deep migration" that
re-reads WAV headers is deferred — it would require the original corpus to
be present and is rarely needed for legacy CSV caches.
}
\keyword{internal}
