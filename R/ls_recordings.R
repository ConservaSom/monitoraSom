#' List of templates
#'
#' A list of two focal recordings of Basileuterus culicivorus from which the
#' templates of the `monitoraSom` package examples were derived.
#'
#' @format ## `ls_recordings` A list of `tuneR::Wave` objects containing focal
#'   recordings. Names of the list elements are the names of the `.wav` files,
#'   and correspond to the name code for recordings in the `monitoraSom`
#'   workflow.
"ls_recordings"
