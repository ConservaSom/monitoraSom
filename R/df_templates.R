#' Metadata of templates
#'
#' A data frame containing the metadata of the templates to run the examples of
#' the `monitoraSom` package, after importing them back to the R session with
#' the `fetch_template_metadata()` function.
#'
#' @format ## `df_templates` A data frame with 6 rows and 11 columns containing
#'   template metadata, including file paths, names, labels, temporal bounds,
#'   sample rates, frequency ranges, and spectrogram parameters (window length
#'   and overlap).
"df_templates"
